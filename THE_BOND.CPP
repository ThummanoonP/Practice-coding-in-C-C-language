#include <stdio.h>
#include <conio.h>
#include <string.h>
struct data
{
	int cp[64];
}; data mi[64];

void main()
{
   int a,b,c,d[64];
   double n=0;
   clrscr();
   scanf("%d",&a);
   for(b=0;b<a;b++)
   {
	  for(c=0;c<a;c++) scanf("%d",&mi[b].cp[c]);
   }
   for(b=0){
   }
   getch();
}